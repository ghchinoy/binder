## What & why

<!-- One or two sentences. -->

## Docs impact — required

**Does this PR change user-visible output?** — prose the binary prints; JSON
keys, values, or schema shape; exit codes; flags, flag names, flag defaults, or
help text; command names.

- [ ] No — none of the above changed.
- [ ] Yes — and the docs task is named below.

**Docs task (required when "Yes"):** _______________________________________

The docs task must land **this same cycle** — in this PR, or a paired PR merged
the same cycle. A backlog issue, a later follow-up, or a release-notes line does
**not** count as the docs task.

> **Reviewers: block this PR if this section is deleted, or if neither box above is checked.**

## Checklist

- [ ] One conventional commit; its bare subject line is **66 characters or less**.
- [ ] No `Release-As:` footer.
- [ ] Any example command output pasted into docs was regenerated from a real
      binary, not hand-edited.
