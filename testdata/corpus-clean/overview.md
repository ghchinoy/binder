# Revenue Knowledge Base

A tiny, fully-resolvable corpus used for the differential interop gate: every
internal link points at a real concept, so both `binder validate` and the
external `okf validate` agree it is conformant.

See the [revenue metric](metrics/revenue.md) for the canonical definition.
